# Contributing to `@trucklinemp/sdk`

The full contributing guide lives on the TrucklineMP docs site:

**→ [Contributing to the TypeScript SDK](https://docs.trucklinemp.com/guides/developers/sdk-contributing/)**

Usage documentation:

**→ [TypeScript SDK](https://docs.trucklinemp.com/guides/developers/sdk/)**

Quick check before a PR:

```bash
npm install
npm run typecheck
npm test
npm run build
```
